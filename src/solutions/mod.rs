pub mod roman_to_integer;

#[allow(dead_code)]
pub struct Solution {
    _na: String,
}

impl Default for Solution {
    #[allow(dead_code)]
    fn default() -> Solution {
        Solution {
            _na: "".to_string(),
        }
    }
}
